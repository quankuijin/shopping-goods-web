server:
  port: 8080

spring:
  application:
    name: shopping-goods
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: foobared
      database: 0
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms

# 登录配置
auth:
  username: admin
  password: "123"
  session-timeout: 3600

# CORS配置
cors:
  allowed-origins: "*"
  allowed-methods: "*"
  allowed-headers: "*"
